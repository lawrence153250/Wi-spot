In order for everything to work, 
First paste the entire repository in a php hosting site.
Then paste the sql commands to the database.
Open the api_keys.js then change the api key for the deepseek. 
Next step is to open the api_keys.php then change all the api keys for deepseek, xendit, and googlemaps.
